# Optional dependencies for enhanced AWS profile enumeration
# The native messaging host works without these, but boto3 provides better
# SSO profile detection and enumeration
boto3>=1.26.0
botocore>=1.29.0

# Profile caching for fast popup loading
tinydb>=4.8.0

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
